spring.application.name=backend

# MySQL数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/study_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=869589
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# 事务管理配置
spring.transaction.default-timeout=30s
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=false

# 服务器配置
server.port=8080

# 日志级别
logging.level.org.springframework.security=DEBUG
logging.level.com.XuebaoMaster.backend=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.transaction=DEBUG

# JWT配置
jwt.secret=yourSecretKeyHereMakeSureItIsAtLeast256BitsLongForHS256Algorithm
jwt.expiration=86400000

# Jackson配置
spring.jackson.serialization.fail-on-empty-beans=false

# 文件上传配置
file.upload-dir=./uploads

# 文件上传大小限制
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# 聊天API配置
chat.api.url=http://localhost:80/v1/chat-messages
chat.api.key=app-WaWtrJx4ibDv6CkFpEagfnC8

# DeepSeek API配置
deepseek.api.url=https://api.deepseek.com/v1/chat/completions
deepseek.api.key=sk-6bfa57029fbc44b9ac4eb38dd61bc695
deepseek.api.model=deepseek-chat
deepseek.api.temperature=0.7
deepseek.api.max-tokens=2048

# RAG服务相关超时配置
spring.mvc.async.request-timeout=1800000

# RestTemplate超时配置
rest.template.connect-timeout=180000
rest.template.read-timeout=1800000

# 教案生成配置
teaching-plan-generator.api-base-url=http://localhost
teaching-plan-generator.api-endpoint=/v1/chat-messages
teaching-plan-generator.token=app-WaWtrJx4ibDv6CkFpEagfnC8
teaching-plan-generator.storage-directory=${user.dir}/uploads/teaching-plans
teaching-plan-generator.user-id=abc-123

# 允许异步处理教案生成
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=25
spring.task.execution.thread-name-prefix=teaching-plan-async-

# 增加响应超时时间以支持长时间运行的API请求
spring.mvc.async.request-timeout=900000
resttemplate.connection-timeout=30000
resttemplate.read-timeout=900000
